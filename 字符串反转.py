# 在这里写上你的代码 :-)
a="abcdefg123456" #定义名称为a的字符串
b=reversed(a)  # 将a的内容反转
print(list(b))  # 将b以列表方式输出
print(b)#只显示内存地址

x=["项","道","德"]  # [1,2,3,4,5] #以[,,,]形式组织的叫列表(list)
y=reversed(x) # 将x的内容反转
print(y) #只显示内存地址
print(list(y))# 将y以列表方式输出

